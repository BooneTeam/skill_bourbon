# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
development:
  adapter: mysql2
  encoding: utf8
  database: skill_climb_development
  username: root
  password: Mouserat1
  socket: /tmp/mysql2.sock
  port: 3307
  reconnect: true

test:
  adapter: mysql2
  encoding: utf8
  database: skill_climb<%=ENV['TEST_ENV_NUMBER']%> # Needed for parallel_tests
  username: root
  password: Mouserat1
  socket: /tmp/mysql2.sock
  reconnect: true
  port: 3307

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
