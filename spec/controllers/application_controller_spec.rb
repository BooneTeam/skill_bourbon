require 'rails_helper'

RSpec.describe ApplicationController, :type => :controller do

it "redirects back to the referral page after login" do
  # after_sign_in_path_for(resource)
end


it "redirects to sign up path if not logged in" do
  # login_required
end

end
